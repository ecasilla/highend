describe('PageTitleAction: ', () => {
  let action;

  beforeEach(function() {
    action = require('../../actions/PageTitleAction.js');
  });

  it('should be defined', () => {
    expect(action).to.be.an('object');
  });
});
